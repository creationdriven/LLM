# Core dependencies
torch>=2.0.0,<3.0.0
tiktoken>=0.5.0
numpy>=1.24.0,<2.0.0
psutil>=5.9.0
tqdm>=4.65.0

# Optional but recommended
transformers>=4.30.0  # For BERT, RoBERTa, DistilBERT, and T5 tokenization
tensorboard>=2.13.0  # For training visualization

# Optional development dependencies
# torchsummary>=1.5.1  # For model summaries (uncomment if needed)

# Optional evaluation dependencies
# nltk>=3.8  # For BLEU scores (T5 evaluation)
# rouge-score>=0.1.2  # For ROUGE scores (T5 evaluation)

# Optional distributed training
# torch>=2.0.0  # Already included, but needed for distributed training